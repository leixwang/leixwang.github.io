name: Deploy leix.wang website
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master
      with:
        persist-credentials: false
    - name: Install and Build
      run: |
        npm install
        npm run-script build
        cd ./markdown/.vitepress/dist
        echo 'leix.wang' > CNAME
        git init
        git config user.name "leix"
        git config user.email "leix@leix.wang"
        git add -A
        git commit -m 'deploy'
        git push -f https://leixwang:${{ secrets.BUILD_LEIXWEB }}@github.com/leixwang/leixwang.github.io.git master:gh-pages
        cd -